// Cloudflare Pages adapter config.
//
// Install the package when deploying:
//   pnpm --filter @ai-voice/web add -D @opennextjs/cloudflare@0.4.x wrangler
//
// (Pinned to 0.4.x because 0.5.12 currently has a broken pkg.pr.new snapshot
// dependency — revisit when fixed upstream.)
//
// Once installed, replace this stub with:
//
//   import { defineCloudflareConfig } from "@opennextjs/cloudflare";
//   export default defineCloudflareConfig({});
export default {};
